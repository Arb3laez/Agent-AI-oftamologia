apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: oftalmo-system
spec:
  selector:
    app: orchestrator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
